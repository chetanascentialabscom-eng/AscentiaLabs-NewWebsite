# Robots.txt for Production Environment
# Allow all search engines to crawl and index the site

User-agent: *
Allow: /

# Crawl delay (optional - helps prevent server overload)
Crawl-delay: 1

# Block any sensitive directories (uncomment if needed)
# Disallow: /admin/
# Disallow: /private/
# Disallow: /api/

# Sitemap location
Sitemap: https://ascentialabs.com/sitemap_index.xml